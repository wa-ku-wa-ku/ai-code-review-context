# Database Configuration
# For local development, use the standard PostgreSQL port 5432
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/fastapi_starter
# Optional: Explicit sync URL for Alembic (auto-converts async URL if not provided)
# DATABASE_URL_SYNC=postgresql+psycopg://postgres:postgres@localhost:5432/fastapi_starter

# Application Configuration
APP_HOST=127.0.0.1
APP_PORT=8000

# JWT Configuration (optional - defaults are provided)
# SECRET_KEY=your-secret-key-here
# ACCESS_TOKEN_EXPIRE_MINUTES=1440
