# Codex 一行启动命令

阶段 0：

```text
请先阅读 AGENTS.md 和 tasks/stage_0_project_skeleton.md。现在只完成阶段 0，不要实现后续阶段。完成后运行 pytest，并按验收标准逐条汇报。
```

阶段 1：

```text
请先阅读 AGENTS.md 和 tasks/stage_1_file_scanner.md。现在只完成阶段 1，不要实现 AST、SQLite、任务生成和工具接口。完成后运行 pytest，并按验收标准逐条汇报。
```

阶段 2：

```text
请先阅读 AGENTS.md 和 tasks/stage_2_ast_parser.md。现在只完成阶段 2，不要实现 SQLite、任务生成、FastAPI API 和覆盖率。完成后运行 pytest，并按验收标准逐条汇报。
```

阶段 3：

```text
请先阅读 AGENTS.md 和 tasks/stage_3_sqlite_index.md。现在只完成阶段 3，不要实现任务生成、覆盖率和 Agent API。完成后运行 pytest，并按验收标准逐条汇报。
```

阶段 4：

```text
请先阅读 AGENTS.md 和 tasks/stage_4_call_graph_routes.md。现在只完成阶段 4，不要实现评审任务生成和覆盖率追踪。完成后运行 pytest，并按验收标准逐条汇报。
```

阶段 5：

```text
请先阅读 AGENTS.md 和 tasks/stage_5_context_tools.md。现在只完成阶段 5，上下文工具接口必须基于已有索引实现，不要实现覆盖率和最终评审逻辑。完成后运行 pytest，并按验收标准逐条汇报。
```

阶段 6：

```text
请先阅读 AGENTS.md 和 tasks/stage_6_review_tasks.md。现在只完成阶段 6，任务生成不调用 LLM，只基于规则和代码索引。完成后运行 pytest，并按验收标准逐条汇报。
```

阶段 7：

```text
请先阅读 AGENTS.md 和 tasks/stage_7_coverage_acceptance.md。现在只完成阶段 7，实现覆盖率追踪和端到端验收，不要新增无关功能。完成后运行 pytest，并按验收标准逐条汇报。
```
